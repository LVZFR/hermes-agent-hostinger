# Hermes Agent — Configuration & State Backup

Sanitized weekly backup of a Hermes Agent install. Contains config.yaml,
SOUL.md, skills/, memories/, and the tutorial. Excludes all secrets
(.env), credentials (auth.json), databases, logs, sessions, and caches.
Updated automatically by a weekly cron job.
